// Opcional: lógica futura (por ahora solo placeholder)
document.getElementById('btnLogin')?.addEventListener('click', function(e) {
  // Navega a login.html (esto también lo hace el link, pero te dejo el ejemplo)
  window.location.href = 'login.html';
});
